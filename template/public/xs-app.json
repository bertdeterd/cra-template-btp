{
  "welcomeFile": "/index.html",
  "routes": [
    {
      "source": "/sap/(.*)",
      "target": "/sap/(.*)",
      "destination": "<DESTINATION TO YOUR BACKEND>"
    },
    {
      "source": "^(.*)",
      "target": "$1",
      "authenticationType": "xsuaa",
      "service": "html5-apps-repo-rt"
    }
  ]
}